services:
  - type: web
    name: mamacare-backend
    runtime: node
    buildCommand: npm install && npm run build && node dist/scripts/seedPlans.js
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
